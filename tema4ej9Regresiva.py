def crear_cuenta_regresiva(numero_inicial):
    lista = []
    while numero_inicial >= 0:
        lista.append(numero_inicial)
        numero_inicial -= 1
        
    return lista
crear_cuenta_regresiva(7)

# ESTE TMB ME SALIO.
